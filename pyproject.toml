[project]
name = "pokemon-vega-modern-kanto"
version = "0.1.0"
requires-python = ">=3.11"
description = "Reproducible project tooling for Vega + DPE/CFRU + postgame Kanto"

[tool.ruff]
line-length = 100

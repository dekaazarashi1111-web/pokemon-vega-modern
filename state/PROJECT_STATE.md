# Project State（互換案内）

現在状態の正本は `design/current_state.md`、タスク状態の正本は `design/tasks_next.md` です。このファイルへ状態を二重記録しません。

- Current milestone: Upstream reproduction / exact audit / content schema
- Current integration gate: Gate A passed
- Release target: 32 MiB reproducible ROM + clean-ROM-targeted patch

機械可読の入力・上流固定値は `state/source-lock.json`、詳細な生成監査は `reports/generated/` を参照します。

name: pr16-research-activities-20260927
on:
  push:
    branches: [codex/modernization-followup-20260908]
    paths: [.github/workflows/pr16-research-activities-20260927.yml]
permissions:
  contents: write
  actions: read
  pull-requests: read
concurrency:
  group: pr16-research-activities-20260927
  cancel-in-progress: false
jobs:
  record:
    if: github.repository == 'dekaazarashi1111-web/pokemon-vega-modern' && github.ref == 'refs/heads/codex/modernization-followup-20260908'
    runs-on: ubuntu-24.04
    timeout-minutes: 12
    env:
      GITHUB_TOKEN: ${{ github.token }}
      PYTHONDONTWRITEBYTECODE: '1'
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: 成功原本と12画像を照合・未保存参照を訂正・新51検査・固定引継ぎと両ログ
        run: python3 -B scripts/pr16_research_bug_record.py record
      - name: 固定引継ぎとtask graphの整合性
        run: |
          python3 -B scripts/pr16_resume.py check
          python3 -B scripts/validate_task_graph.py
      - name: 今回indexと開始HEADからの全変更を限定guard
        run: python3 -B scripts/pr16_research_bug_record.py guard
      - name: 最新HEAD再確認・commit・同branch非force push
        shell: bash
        run: |
          set -euo pipefail
          test "$(git ls-remote origin "$GITHUB_REF" | cut -f1)" = "$GITHUB_SHA"
          python3 -B -c 'import sys;sys.path[:0]=["scripts","."];import pr16_research_lifecycle_actions as d;d.current()'
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git commit -m "USER-20260927-RESEARCH-ACTIVITIES: 虫取り0→8RP・保存再開・51検査受入と固定引継ぎ完了"
          git push origin HEAD:refs/heads/codex/modernization-followup-20260908
          test "$(git ls-remote origin "$GITHUB_REF" | cut -f1)" = "$(git rev-parse HEAD)"
          echo "RESULT=DONE TASK=USER-20260927-RESEARCH-ACTIVITIES VERIFY=PASS COMMIT=$(git rev-parse HEAD)"
      - name: 次工程向けのcommit済みソースとActionsを保存（native/compile再実行0）
        run: GITHUB_SHA="$(git rev-parse HEAD)" python3 -B scripts/pr16_research_activities_context.py
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: pr16-research-bug-closeout
          path: .local/pr16-research-bug-record/public/
          retention-days: 14
          if-no-files-found: error
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: pr16-research-activities-context
          path: .local/pr16-research-activities/context/
          retention-days: 14
          if-no-files-found: warn

name: pr16-ring-bootstrap-lifecycle-contracts
run-name: font初期化から非空message終了まで同一RAMを検証
on:
  push:
    branches: [codex/modernization-followup-20260908]
    paths:
      - .github/workflows/pr16-ring-bootstrap-lifecycle-contracts.yml
      - scripts/pr16_ring_bootstrap_lifecycle_contracts.py
      - tests/test_pr16_ring_bootstrap_lifecycle_contracts.py
  workflow_dispatch:
permissions:
  contents: write
  actions: read
  pull-requests: read
concurrency:
  group: pr16-ring-bootstrap-lifecycle-contracts
  cancel-in-progress: false
jobs:
  focused:
    if: github.repository == 'dekaazarashi1111-web/pokemon-vega-modern' && github.ref == 'refs/heads/codex/modernization-followup-20260908'
    runs-on: ubuntu-24.04
    timeout-minutes: 25
    env:
      GH_TOKEN: ${{ github.token }}
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ github.sha }}
          fetch-depth: 0
      - name: 保存初期化とmessage終了を検証し同branchへ記録
        run: python3 scripts/pr16_ring_bootstrap_lifecycle_contracts.py run
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: pr16-ring-bootstrap-lifecycle-contracts
          path: |
            .local/pr16-ring-bootstrap-lifecycle-contracts/analysis.json
            .local/pr16-ring-bootstrap-lifecycle-contracts/tests.json
            .local/pr16-ring-bootstrap-lifecycle-contracts/tests.txt
            .local/pr16-ring-bootstrap-lifecycle-contracts/guard.json
            .local/pr16-ring-bootstrap-lifecycle-contracts/recorded-result.json
            .local/pr16-ring-bootstrap-lifecycle-contracts/export/
          include-hidden-files: true
          if-no-files-found: error
          retention-days: 90

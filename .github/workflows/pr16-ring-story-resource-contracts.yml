name: pr16-ring-story-resource-contracts
run-name: 実BG供給・通常window連続RAMとsave退避停止の351条件
on:
  push:
    branches: [codex/modernization-followup-20260908]
    paths:
      - .github/workflows/pr16-ring-story-resource-contracts.yml
      - scripts/pr16_ring_story_resource_contracts.py
      - scripts/pr16_ring_story_resource_machine.py
      - tests/test_pr16_ring_story_resource_contracts.py
  workflow_dispatch:
permissions:
  contents: write
  actions: read
  pull-requests: read
concurrency:
  group: pr16-ring-story-resource-contracts
  cancel-in-progress: false
jobs:
  focused:
    if: github.repository == 'dekaazarashi1111-web/pokemon-vega-modern' && github.ref == 'refs/heads/codex/modernization-followup-20260908'
    runs-on: ubuntu-24.04
    timeout-minutes: 20
    env:
      GH_TOKEN: ${{ github.token }}
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ github.sha }}
          fetch-depth: 0
      - name: 351条件と40テストを検証し同branchへ記録
        run: python3 scripts/pr16_ring_story_resource_contracts.py run
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: pr16-ring-story-resource-contracts
          path: |
            .local/pr16-ring-story-resource-contracts/analysis.json
            .local/pr16-ring-story-resource-contracts/tests.json
            .local/pr16-ring-story-resource-contracts/tests.txt
            .local/pr16-ring-story-resource-contracts/guard.json
            .local/pr16-ring-story-resource-contracts/recorded-result.json
            .local/pr16-ring-story-resource-contracts/export/
          include-hidden-files: true
          if-no-files-found: error
          retention-days: 90

name: pr16-shared-egg-routes
run-name: New shared-only physical memory, all four slots, cancels and cold saves
on:
  push:
    branches: [codex/modernization-followup-20260908]
    paths:
      - .github/workflows/pr16-shared-egg-routes.yml
      - scripts/pr16_shared_egg_routes.py
      - tests/test_pr16_shared_egg_routes.py
  workflow_dispatch:
permissions:
  contents: read
concurrency:
  group: pr16-shared-egg-routes
  cancel-in-progress: false
jobs:
  shared-egg:
    if: github.repository == 'dekaazarashi1111-web/pokemon-vega-modern' && github.ref == 'refs/heads/codex/modernization-followup-20260908'
    runs-on: ubuntu-24.04
    timeout-minutes: 25
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ github.sha }}
          persist-credentials: false
      - name: Exact HEAD, toolchain and existing Stage84 parent
        run: |
          set -euo pipefail
          mkdir -p .local/pr16-shared-evidence
          git rev-parse HEAD > .local/pr16-shared-evidence/tested-head.txt
          bash infra/setup_github_actions.sh --install > .local/pr16-shared-evidence/toolchain.log 2>&1
          python3 scripts/modernization_final_integration.py prepare > .local/pr16-shared-evidence/stage84.json
      - name: Restore only the exact previously adopted V4 source
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          set -euo pipefail
          mkdir -p .local/pr16-inputs
          gh release download private-environment-v1 --pattern pokemon-vega-private-env-v1-inputs.zip --dir .local/pr16-inputs
          python3 - <<'PY'
          import hashlib,json,pathlib,zipfile
          root=pathlib.Path('.')
          cfg=json.loads((root/'config/github_private_environment.json').read_text())
          model=next(a for a in cfg['archives'] if a['name']=='pokemon-vega-private-env-v1-inputs.zip')
          archive=root/'.local/pr16-inputs'/model['name'];raw=archive.read_bytes()
          assert len(raw)==model['size'] and hashlib.sha256(raw).hexdigest()==model['sha256']
          name='userfile/imports/Pokemon-Vega_MOVE-DISTRIBUTION-V4_IMPLEMENTATION-READY.zip'
          with zipfile.ZipFile(archive) as z:data=z.read(name)
          assert hashlib.sha256(data).hexdigest()=='4022cd6e1358f58dffc5ebc38b756166f0a1072f948af6934298f65bd82678b2'
          target=root/name
          assert not any(p.is_symlink() for p in (target,*target.parents))
          if target.exists():assert target.read_bytes()==data
          else:target.parent.mkdir(parents=True,exist_ok=True);target.write_bytes(data);target.chmod(0o444)
          PY
      - name: Reconstruct frozen candidate and check independent oracle negatives
        run: |
          set -euo pipefail
          out=.local/pr16-shared-evidence
          python3 scripts/pr16_p07_preserved_layer.py > "$out/p07.stdout" 2> "$out/p07.stderr"
          python3 scripts/pr16_evolution_learning_repair.py > "$out/repair.stdout" 2> "$out/repair.stderr"
          python3 -m unittest discover -s tests -p test_pr16_shared_egg_routes.py -v > "$out/unit.log" 2>&1
          cp .local/pr16-evolution-repair/candidate.json "$out/candidate.json"
      - name: New normal Bag memory input, shared-only selection, save and fresh Continue
        run: |
          set -euo pipefail
          python3 scripts/pr16_shared_egg_routes.py > .local/pr16-shared-evidence/native.stdout 2> .local/pr16-shared-evidence/native.stderr
          git diff --exit-code
      - name: Preserve originals and executed source, never ROM or save
        if: always()
        run: |
          python3 - <<'PY'
          import hashlib,json,pathlib,subprocess,zipfile
          root=pathlib.Path('.');out=root/'.local/pr16-shared-evidence';out.mkdir(parents=True,exist_ok=True)
          names=['scripts/pr16_shared_egg_routes.py','tests/test_pr16_shared_egg_routes.py','.github/workflows/pr16-shared-egg-routes.yml','scripts/pr16_repaired_acceptance.py','scripts/pr16_integrated_native.py','scripts/run_modernization_p03_relearner_e2e.py','tools/mgba_modernization_p03_relearner_e2e.c','scripts/pr16_receiver_audit.py','tests/test_pr16_receiver_audit.py']
          bindings={}
          with zipfile.ZipFile(out/'sources.zip','w',zipfile.ZIP_DEFLATED) as z:
              for name in names:
                  p=root/name;raw=p.read_bytes();raw.decode('utf8');assert not p.is_symlink() and b'\0' not in raw
                  z.writestr(name,raw);bindings[name]={'size':len(raw),'sha256':hashlib.sha256(raw).hexdigest()}
          (out/'source-bindings.json').write_text(json.dumps(bindings,sort_keys=True,indent=2)+'\n')
          (out/'tested-head.txt').write_text(subprocess.check_output(['git','rev-parse','HEAD'],text=True))
          PY
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: pr16-shared-egg-acceptance
          path: |
            .local/pr16-shared-evidence/
            .local/pr16-shared-egg-routes/
          include-hidden-files: true
          if-no-files-found: error
          retention-days: 30

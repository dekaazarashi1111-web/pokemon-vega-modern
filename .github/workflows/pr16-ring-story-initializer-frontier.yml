name: pr16-ring-story-initializer-frontier
run-name: 実field初期化calleeとwindow/printerの限定接続
on:
  push:
    branches: [codex/modernization-followup-20260908]
    paths:
      - .github/workflows/pr16-ring-story-initializer-frontier.yml
      - scripts/pr16_ring_story_initializer_frontier.py
      - tests/test_pr16_ring_story_initializer_frontier.py
  workflow_dispatch:
permissions:
  contents: write
  actions: read
  pull-requests: read
concurrency:
  group: pr16-ring-story-initializer-frontier
  cancel-in-progress: false
jobs:
  focused:
    if: github.repository == 'dekaazarashi1111-web/pokemon-vega-modern' && github.ref == 'refs/heads/codex/modernization-followup-20260908'
    runs-on: ubuntu-24.04
    timeout-minutes: 25
    env:
      GH_TOKEN: ${{ github.token }}
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ github.sha }}
          fetch-depth: 0
      - name: field初期化の未読calleeを採取し同branchへ記録
        run: python3 scripts/pr16_ring_story_initializer_frontier.py run
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: pr16-ring-story-initializer-frontier
          path: |
            .local/pr16-ring-story-initializer-frontier/preflight.json
            .local/pr16-ring-story-initializer-frontier/analysis.json
            .local/pr16-ring-story-initializer-frontier/tests.json
            .local/pr16-ring-story-initializer-frontier/tests.txt
            .local/pr16-ring-story-initializer-frontier/guard.json
            .local/pr16-ring-story-initializer-frontier/recorded-result.json
            .local/pr16-ring-story-initializer-frontier/export/
          include-hidden-files: true
          if-no-files-found: error
          retention-days: 90

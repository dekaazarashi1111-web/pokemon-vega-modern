name: modernization-stage83-mgba
run-name: Stage79 mGBA (Stage83 decided P06, seven fresh domains)
on:
  push:
    branches: [codex/modernization-followup-20260908]
    paths:
      - .github/workflows/modernization-stage83-mgba.yml
      - scripts/run_modernization_stage83_github_domain.py
      - tests/test_modernization_stage83_github_domain.py
      - tools/modernization_p06_decided_adjustments.py
      - content/modernization/p06_decided_adjustments.json
  workflow_dispatch:
permissions:
  contents: read
concurrency:
  group: modernization-stage83-mgba-${{ github.ref }}
  cancel-in-progress: false
jobs:
  plan:
    runs-on: ubuntu-24.04
    timeout-minutes: 10
    outputs:
      fingerprint: ${{ steps.plan.outputs.fingerprint }}
    steps:
      - uses: actions/checkout@v4
        with:
          persist-credentials: false
      - name: Fixed toolchain for reproducible Stage82 assembly
        run: bash infra/setup_github_actions.sh --install
      - name: Regression tests for exact candidate and unchanged contracts
        run: |
          set -euo pipefail
          mkdir -p .local/stage83-plan
          python3 -m unittest discover -s tests -p test_modernization_stage83_github_domain.py -v > .local/stage83-plan/tests.log 2>&1
      - id: plan
        name: Validate full parent chain and Stage83 matrix plan
        run: |
          set -euo pipefail
          python3 scripts/run_modernization_stage83_github_domain.py prepare > .local/stage83-plan/prepare.json
          python3 scripts/run_modernization_stage83_github_domain.py plan > .local/stage83-plan/plan.json
          cp .local/stage83-decided-species/config.json .local/stage83-plan/config.json
          cp .local/stage83-decided-species/candidate.json .local/stage83-plan/candidate.json
          git rev-parse HEAD > .local/stage83-plan/tested-head.txt
          echo "fingerprint=$(python3 -c 'import json;print(json.load(open(".local/stage83-plan/plan.json"))["plan_fingerprint"])')" >> "$GITHUB_OUTPUT"
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: stage83-plan
          path: .local/stage83-plan/
          include-hidden-files: true
          if-no-files-found: error
          retention-days: 14
  domain:
    needs: plan
    runs-on: ubuntu-24.04
    timeout-minutes: 35
    strategy:
      fail-fast: false
      matrix:
        domain: [p02, mega_shop, floette, p03, p04_mega_runtime, battle_policy, p05]
    steps:
      - uses: actions/checkout@v4
        with:
          persist-credentials: false
      - name: Fixed toolchain
        run: |
          set -euo pipefail
          mkdir -p .local/stage83-domain
          bash infra/setup_github_actions.sh --install > .local/stage83-domain/toolchain.log 2>&1
      - name: Verify identical Stage83 plan
        env:
          EXPECTED_FINGERPRINT: ${{ needs.plan.outputs.fingerprint }}
        run: |
          set -euo pipefail
          python3 scripts/run_modernization_stage83_github_domain.py prepare > .local/stage83-domain/prepare.json
          python3 scripts/run_modernization_stage83_github_domain.py plan > .local/stage83-domain/plan.json
          python3 -c 'import json,os; p=json.load(open(".local/stage83-domain/plan.json")); assert p["plan_fingerprint"] == os.environ["EXPECTED_FINGERPRINT"]'
          git rev-parse HEAD > .local/stage83-domain/tested-head.txt
      - name: Fresh mGBA execution with unchanged Stage79 contract
        env:
          DOMAIN: ${{ matrix.domain }}
        run: |
          set -euo pipefail
          python3 scripts/run_modernization_stage83_github_domain.py run-domain --domain "$DOMAIN" --output-directory .local/stage83-domain > .local/stage83-domain/job-summary.json 2> .local/stage83-domain/job.stderr.log
          python3 scripts/run_modernization_stage83_github_domain.py validate-domain --domain "$DOMAIN" --record .local/stage83-domain/result.json > .local/stage83-domain/validation.json
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: stage83-domain-${{ matrix.domain }}
          path: .local/stage83-domain/
          include-hidden-files: true
          if-no-files-found: error
          retention-days: 14
  merge:
    needs: [plan, domain]
    runs-on: ubuntu-24.04
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
        with:
          persist-credentials: false
      - name: Fixed toolchain for reproducible candidate validation
        run: bash infra/setup_github_actions.sh --install
      - uses: actions/download-artifact@v4
        with:
          pattern: stage83-domain-*
          path: .local/stage83-input
      - name: Merge and check all seven results, preserving historical gate
        run: |
          set -euo pipefail
          mkdir -p .local/stage83-merged
          python3 scripts/run_modernization_stage83_github_domain.py prepare > .local/stage83-merged/prepare.json
          python3 scripts/run_modernization_stage83_github_domain.py merge --input-directory .local/stage83-input --output-directory .local/stage83-merged > .local/stage83-merged/merge-summary.json
          git diff --exit-code -- config/active_play_baseline.json content/modernization/stage79_cumulative_mgba_runtime_gate.json build/stages
          git rev-parse HEAD > .local/stage83-merged/tested-head.txt
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: stage83-cumulative-result
          path: .local/stage83-merged/
          include-hidden-files: true
          if-no-files-found: error
          retention-days: 14

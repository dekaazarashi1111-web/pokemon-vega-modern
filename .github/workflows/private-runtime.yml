name: private-runtime
run-name: private-runtime (${{ inputs.suite }})

on:
  workflow_dispatch:
    inputs:
      suite:
        description: 実行するprivate環境検証
        required: true
        type: choice
        default: battle-cli-offline
        options:
          - battle-cli-offline
          - stage62-check
          - stage62-mgba
          - full-unit
          - all

permissions:
  contents: read

concurrency:
  group: private-runtime-${{ github.ref }}
  cancel-in-progress: false

jobs:
  verify:
    runs-on: ubuntu-24.04
    timeout-minutes: 360
    steps:
      - uses: actions/checkout@v4
      - name: 固定toolchainを導入
        run: bash infra/setup_github_actions.sh --install
      - name: Private Release資材を取得
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          set -euo pipefail
          tag=$(python3 -c 'import json; print(json.load(open("config/github_private_environment.json"))["release"]["tag"])')
          mkdir -p .local/github-private-environment/assets
          gh release download "$tag" \
            --pattern 'pokemon-vega-private-env-v1-*.zip' \
            --dir .local/github-private-environment/assets
      - name: Private環境をhash検証して復元
        run: >-
          python3 scripts/github_private_environment.py restore
          --archive-dir .local/github-private-environment/assets
      - name: 選択suiteを実行
        env:
          SUITE: ${{ inputs.suite }}
        run: python3 scripts/run_github_private_suite.py "$SUITE"

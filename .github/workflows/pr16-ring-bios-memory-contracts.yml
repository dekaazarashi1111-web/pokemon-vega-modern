name: pr16-ring-bios-memory-contracts
run-name: BIOSコピー・fillの供給元とメモリ効果を未観測suffixへ結合
on:
  push:
    branches: [codex/modernization-followup-20260908]
    paths:
      - .github/workflows/pr16-ring-bios-memory-contracts.yml
      - scripts/pr16_ring_bios_memory_contracts.py
      - scripts/pr16_ring_bios_record.py
      - tests/test_pr16_ring_bios_memory_contracts.py
  workflow_dispatch:
permissions:
  contents: write
  actions: read
  pull-requests: read
concurrency:
  group: pr16-ring-bios-memory-contracts
  cancel-in-progress: false
jobs:
  focused:
    if: github.repository == 'dekaazarashi1111-web/pokemon-vega-modern' && github.ref == 'refs/heads/codex/modernization-followup-20260908'
    runs-on: ubuntu-24.04
    timeout-minutes: 25
    env:
      GH_TOKEN: ${{ github.token }}
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ github.sha }}
          fetch-depth: 0
      - name: 新規BIOS契約を検証し同branchへ記録
        run: python3 scripts/pr16_ring_bios_memory_contracts.py run
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: pr16-ring-bios-memory-contracts
          path: |
            .local/pr16-ring-bios-memory-contracts/analysis.json
            .local/pr16-ring-bios-memory-contracts/tests.json
            .local/pr16-ring-bios-memory-contracts/tests.txt
            .local/pr16-ring-bios-memory-contracts/preflight.json
            .local/pr16-ring-bios-memory-contracts/guard.json
            .local/pr16-ring-bios-memory-contracts/recorded-result.json
            .local/pr16-ring-bios-memory-contracts/export/
          include-hidden-files: true
          if-no-files-found: error
          retention-days: 90

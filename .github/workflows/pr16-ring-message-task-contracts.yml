name: pr16-ring-message-task-contracts
run-name: message task状態2終了と上流scriptの1231条件結合
on:
  push:
    branches: [codex/modernization-followup-20260908]
    paths:
      - .github/workflows/pr16-ring-message-task-contracts.yml
      - scripts/pr16_ring_message_task_contracts.py
      - tests/test_pr16_ring_message_task_contracts.py
  workflow_dispatch:
permissions:
  contents: write
  actions: read
  pull-requests: read
concurrency:
  group: pr16-ring-message-task-contracts
  cancel-in-progress: false
jobs:
  focused:
    if: github.repository == 'dekaazarashi1111-web/pokemon-vega-modern' && github.ref == 'refs/heads/codex/modernization-followup-20260908'
    runs-on: ubuntu-24.04
    timeout-minutes: 20
    env:
      GH_TOKEN: ${{ github.token }}
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ github.sha }}
          fetch-depth: 0
      - name: 上流scriptとtask終了の限定結合を検証し同branchへ記録
        run: python3 scripts/pr16_ring_message_task_contracts.py run
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: pr16-ring-message-task-contracts
          path: |
            .local/pr16-ring-message-task-contracts/export/
            .local/pr16-ring-message-task-contracts/analysis.json
            .local/pr16-ring-message-task-contracts/tests.json
            .local/pr16-ring-message-task-contracts/tests.txt
            .local/pr16-ring-message-task-contracts/recorded-result.json
          include-hidden-files: true
          if-no-files-found: error
          retention-days: 90

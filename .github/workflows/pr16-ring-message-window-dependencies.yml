name: pr16-ring-message-window-dependencies
run-name: message状態0/1の残存5依存を限定保存
on:
  push:
    branches: [codex/modernization-followup-20260908]
    paths:
      - .github/workflows/pr16-ring-message-window-dependencies.yml
      - scripts/pr16_ring_message_window_dependencies.py
      - tests/test_pr16_ring_message_window_dependencies.py
  workflow_dispatch:
permissions:
  contents: write
  actions: read
  pull-requests: read
concurrency:
  group: pr16-ring-message-window-dependencies
  cancel-in-progress: false
jobs:
  focused:
    if: github.repository == 'dekaazarashi1111-web/pokemon-vega-modern' && github.ref == 'refs/heads/codex/modernization-followup-20260908'
    runs-on: ubuntu-24.04
    timeout-minutes: 20
    env:
      GH_TOKEN: ${{ github.token }}
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ github.sha }}
          fetch-depth: 0
      - name: 未読5依存を検証し同branchへ記録
        run: python3 scripts/pr16_ring_message_window_dependencies.py run
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: pr16-ring-message-window-dependencies
          path: |
            .local/pr16-ring-message-window-dependencies/export/
            .local/pr16-ring-message-window-dependencies/analysis.json
            .local/pr16-ring-message-window-dependencies/tests.json
            .local/pr16-ring-message-window-dependencies/tests.txt
            .local/pr16-ring-message-window-dependencies/recorded-result.json
          include-hidden-files: true
          if-no-files-found: error
          retention-days: 90

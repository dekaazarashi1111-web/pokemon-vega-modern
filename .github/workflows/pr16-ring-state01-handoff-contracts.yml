name: pr16-ring-state01-handoff-contracts
run-name: 同一RAMのstate0→1→2とqueue引継ぎを検証
on:
  push:
    branches: [codex/modernization-followup-20260908]
    paths:
      - .github/workflows/pr16-ring-state01-handoff-contracts.yml
      - scripts/pr16_ring_state01_handoff_contracts.py
      - tests/test_pr16_ring_state01_handoff_contracts.py
  workflow_dispatch:
permissions:
  contents: write
  actions: read
  pull-requests: read
concurrency:
  group: pr16-ring-state01-handoff-contracts
  cancel-in-progress: false
jobs:
  focused:
    if: github.repository == 'dekaazarashi1111-web/pokemon-vega-modern' && github.ref == 'refs/heads/codex/modernization-followup-20260908'
    runs-on: ubuntu-24.04
    timeout-minutes: 25
    env:
      GH_TOKEN: ${{ github.token }}
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ github.sha }}
          fetch-depth: 0
      - name: 同一RAM連続経路を検証し同branchへ記録
        run: python3 scripts/pr16_ring_state01_handoff_contracts.py run
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: pr16-ring-state01-handoff-contracts
          path: |
            .local/pr16-ring-state01-handoff-contracts/analysis.json
            .local/pr16-ring-state01-handoff-contracts/tests.json
            .local/pr16-ring-state01-handoff-contracts/tests.txt
            .local/pr16-ring-state01-handoff-contracts/preflight.json
            .local/pr16-ring-state01-handoff-contracts/guard.json
            .local/pr16-ring-state01-handoff-contracts/recorded-result.json
            .local/pr16-ring-state01-handoff-contracts/export/
          include-hidden-files: true
          if-no-files-found: error
          retention-days: 90

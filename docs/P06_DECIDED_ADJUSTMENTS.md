# P06：受領済みの決定2件をStage83候補へ反映

従来のP06 checkpointは「ジバクンの特性差替え」「カモナイツA45」を仕様未提出と扱っていました。Libraryの2026-09-04監査と習得監査にある「決定済みナーフ」の原文を照合し、通常特性2枠と攻撃1項目だけを採用しました。2資料の原文を `design/imported/p06_decided_20260904/` へ不変で保存し、サイズ・SHA-256を採用JSONから検証します。

|対象|採用内容|
|---|---|
|ジバクン `SPECIES_KEY_VEGA_220` / base|通常1: ふしぎなまもり → のろわれボディ。通常2: かげふみ → おみとおし。|
|カモナイツ `SPECIES_KEY_VEGA_373` / base|攻撃75 → 45。他の5能力値・特性・タイプは維持。|

採用正本: `content/modernization/p06_decided_adjustments.json`。これは回収した決定に基づく追補です。旧 `p06_species_adjustment_contract.json` と194件の復元候補は当時の未採用checkpointとして保存し、そのレビューを一括採用しません。

Stage82を既存の固定生成経路で再構築し、その全体ハッシュ・BaseStatsの現在の参照先・対象2種の全32バイト・元の各値を照合した後、別のStage83候補を生成します。実差分は3バイトです。種族ID・特性ID・タイプ・隠れ特性・技配布・保存ABIを変更しません。出力ROMと使用するセーブはGit管理外です。

`python3 scripts/run_modernization_p06_decided_e2e.py` は8件を新しいmGBAプロセスで検証します。2種×通常2枠×「親ROMの既存個体／候補ROMの新規個体」です。各ケースは通常Save→別core Continue→通常BagからアメでLv48→49→通常Save→第3core Continueを通ります。保存/Continue/アメ操作中はホストのメモリ・レジスタ書換え7 APIを禁止し、対象処理を直接呼びません。

初期個体・道具は隔離fixtureです。特性IDの確認は観測区間外のnative ABI呼出しで行い、戦闘効果や自然な捕獲の検証とは数えません。能力実数値の期待値はROMの計算関数を呼んで取得せず、種族値・性格・IV・EV・レベルから独立に計算します。既存セーブの100バイトはContinue時にそのまま保持され、保存済みの能力実数値は通常の再計算契機まで維持されます。アメ使用後の値と保存・再起動後の値も照合します。

この2件を採用0件と数え続けることはしませんが、P06の全レビュー、P07の技割当、P05の戦闘効果・施設入場、最終リリースを完了扱いにもしません。Stage62の実プレイ基準、実プレイセーブ、PRのDraft・未マージ状態は維持します。

# PR16 道具取得→装備→戦闘：経路調査の再開点

この文書は経路調査のWIP記録です。道具接続の実ROM受入、P05全体、製品リリースを成功へ変更するものではありません。既存の受入JSONと原本を上書きしません。

## この作業で応答を確認できた保存

- 調査コード追加commit: `12394d4dbc9b61a8958c8bc0f69911b29f2bac0f`
- 実行workflow追加commit: `665bd5e82c0cf2492c98cc8d9748ee6f160d10e2`
- Workflow: `.github/workflows/pr16-gear-route-probe.yml`
- Run: `34579454070`
- Runの実行HEAD: `665bd5e82c0cf2492c98cc8d9748ee6f160d10e2`
- Artifact: `pr16-gear-route-probe` / ID `10191015811`
- Actionsが返したartifactサイズ: `16626310` bytes
- Actionsが返したartifact SHA-256: `12a465f0eb375b6febb8e8eb0c998aaf8425f476c2500080b6a5444b4617688c`

artifactの存在・メタデータとダウンロード応答までは取得しました。全jobの最終conclusionと、このartifact自体のGit追跡保存は、この文書では確認済みにしていません。静的な経路調査をエミュレータ実行件数に加えません。

## 続ける対象

`NATURAL_CAPTURE_GEAR` の未受入部分、すなわち実取得した石の通常装備から戦闘への接続です。経路調査では店から北側の草むらへ通常歩行でつながる候補経路を見つけています。まず上記artifactに含まれる経路出力と固定ソースを読み、既存の購入操作・装備メニュー・自然遭遇のコードを再利用してください。同じ経路調査や既存捕獲試験を理由なく再実行しないでください。

必要な実操作は、受付との会話→商品選択・購入→通常の道具を持たせる操作→実歩行→自然遭遇→取得・装備した同じ道具を使う戦闘→通常保存→別コアContinueです。開始地点・BP・リング・手持ちなど、事前に用意した条件はfixtureとして明記し、それら自体の通常入手まで受入済みとはしません。観測開始後の道具・所持金・個体・位置・遭遇・戦闘状態の注入で経路をつないではいけません。

新しいコードを追加する前に、PR #16の最新HEADと該当ファイルを確認してください。調査後の追加実装が既に存在する場合はそれを継続し、この文書を理由に作り直さないでください。

## 保持する既存成功と境界

- ショップ表示修正・取得・保存の受入は `pr16_shop_display_acceptance.json` を保持します。
- 自然捕獲した同じ個体の実戦・再保存・第3コアContinueは `pr16_captured_battle_acceptance.json`、run `34577360374` の新規2件6コアを保持します。これは道具接続の新規実行ではありません。
- 使用候補SHA-256: `e630f7f199194fb4b531ff3a561da866902aea200770a832aec5c276e1636267`、33554432 bytes、CRC32 `BFB089F9`。親候補の原本を後継候補の新規実行へ読み替えません。
- P06採用2種3項目の完了、P07の1073行追加・499行保持、進化・ロトム・繁殖・共有技等の既存成功は維持します。
- publicと追加技アーカイブの殿堂入り後・Bagわざメモリーから無料という仕様は所有者承認済みです。

入口は引き続き `content/modernization/p08_remaining_work.json` と `docs/PR16_CAPTURED_BATTLE_CHECKPOINT_JA.md` です。本WIP文書だけを根拠に `gear_to_battle_accepted`、各phase全体、`release_ready` をtrueへ変更しません。Stage62・実プレイsave・公開範囲・既存履歴を変更せず、同じPRを未マージで続けます。

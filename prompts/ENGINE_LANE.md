# Engine Lane Prompt

You own tasks T01, T03, T04, T05, T06, T07, T09, and T10 on branch `codex/lane-engine`.

Read root `AGENTS.md` and `WORKSTREAMS.md`. Work only on the current engine `PRIMARY`, or prepare a named engine `PARALLEL_PREP` without changing the root queue, shared logs, or shared config. Preserve Vega IDs, build from clean inputs, and integrate source code rather than forcing Factory UPS over Vega. Commit small, bisectable lane milestones after smoke tests. Do not edit Kanto content manifests except for schema-compatible references. Root integration, task completion, and continuation remain the parent agent's responsibility.
